////  CategoryBreakdownList.swift//  FinanceApp////  Created by user271129 on 5/28/26.//import SwiftUIstruct CategoryBreakdownList: View {    let categories: [CategorySummary]    let total: Decimal    var body: some View {        ForEach(categories) { item in            let percent = calculatePercent(item.total, total)            HStack {                Circle()                    .fill(Color(hex: item.category.color) ?? .gray)                    .frame(width: 12, height: 12)                Text(item.category.name)                Spacer()                Text(item.total.money)                Text("\(percent)%")                    .foregroundColor(.secondary)            }            .padding(.vertical, 4)        }    }    private func calculatePercent(_ value: Decimal, _ total: Decimal) -> String {        guard total != 0 else { return "0%" }        let percent = (value / total) * 100        let rounded = percent.rounded(scale: 2)        return rounded.formatted()    }}
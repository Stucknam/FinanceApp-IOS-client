////  AccountPicker.swift//  FinanceApp////  Created by user271129 on 5/28/26.//import SwiftUIstruct AccountPicker: View {    @Binding var selected: Account?    let accounts: [Account]    let totalBalance: Decimal    var body: some View {        Menu {            Button {                selected = nil            } label: {                HStack {                    Text("Все счета")                    Spacer()                    Text(totalBalance.money)                        .foregroundColor(.secondary)                }            }            ForEach(accounts) { acc in                Button {                    selected = acc                } label: {                    HStack {                        Text(acc.name)                        Spacer()                        Text(acc.amount.money)                            .foregroundColor(.secondary)                    }                }            }        } label: {            HStack {                VStack(alignment: .leading, spacing: 2) {                    Text(selected?.name ?? "Все счета")                        .font(.headline)                    Text(selected?.amount.money ?? totalBalance.money)                        .font(.subheadline)                        .foregroundColor(.secondary)                }                Spacer()                Image(systemName: "chevron.down")                    .foregroundColor(.secondary)            }            .padding(.vertical, 6)        }    }}